/**
 * Background — clean light theme version.
 * No particles, no glow orbs. The background color is set by CSS on body.
 * This component is kept as a placeholder so App.jsx import stays intact.
 */
export default function Background() {
  return null
}
